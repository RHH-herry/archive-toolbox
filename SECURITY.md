# 安全文档 · 归档 (archive-toolbox)

> 版本：1.0.0 正式版（serial `AT-1.0.0-20260808-001`） · 更新日期：2026-08-08
> 维护者：RHH-herry · 许可证：MIT

## 1. 设计原则：本地优先（Local-first）

本工具的核心安全主张是 **「文件绝不出本机」**。所有压缩、解压、校验、拆分/合并均在用户设备的浏览器或 Electron 渲染进程内完成，**不发起任何指向外部服务器的网络请求**。

- 网页版（双击 `index.html`）与桌面版（Electron 33）共用同一套前端逻辑（`app.js` + `vendor/`）。
- 校验引擎 `vendor/hash.js`：MD5 为**手写零依赖实现**，SHA 系列使用浏览器原生 `crypto.subtle`（本地计算，不联网）。
- 压缩/解压依赖 `vendor/jszip.min.js`，**本地 vendored 引入，无 CDN、无运行时外部脚本**。

## 2. 数据流与隐私边界

| 操作 | 数据去向 | 是否离开本机 |
|------|----------|--------------|
| 压缩 / 解压 / 校验 / 拆分合并 | 浏览器内存 / 用户所选文件夹 / 本机下载目录 | 否 |
| 「最近记录」 | `localStorage`（仅存文件名、大小、时间，不含文件内容） | 否 |
| 主题偏好 | `localStorage` | 否 |
| 解压写入 | 经 **File System Access API** 写入用户**显式选择**的文件夹 | 否（用户授权） |

**结论**：不存在遥测、分析、广告 SDK 或任何 `fetch`/`XMLHttpRequest` 外联调用（已静态审查 `app.js`、`index.html` 确认）。

## 3. 桌面版（Electron）安全基线

| 配置项 | 取值 | 说明 |
|--------|------|------|
| `contextIsolation` | `true` | 渲染进程与 Electron 内部上下文隔离 |
| `nodeIntegration` | `false` | 网页脚本无法直接访问 Node.js / 文件系统 |
| 预加载脚本 (`preload.js`) | 仅经 `contextBridge` 暴露 `minimize/maximize/close` 三个窗口控制 | 最小攻击面 |
| 远程内容 | 无 | 仅加载本地 `file://` 资源 |

窗口控制通过 IPC（`ipcMain.on('win:min'…)`）在 `main.js` 中执行，渲染进程无法直接调用 Node API，**不存在 RCE 路径**。

## 4. 威胁模型

| 威胁 | 可能性 | 现有缓解 |
|------|--------|----------|
| 文件被上传到第三方 | 极低 | 无网络请求；本地优先架构 |
| 恶意压缩包触发代码执行 | 低 | JSZip 仅解析归档条目并写入用户所选目录；无脚本执行 |
| 供应链投毒（CDN 被篡改） | 无 | 全部依赖本地 vendored，无外部 CDN |
| 自动更新被劫持 | 不适用 | 桌面版**无自动更新**；仅手动安装包 / 网页版，无 OTA 供应链风险 |

## 5. 已知限制与建议

- 网页版在**不支持 File System Access API** 的浏览器中，解压/拆分会回退为「逐一下载」，文件落在本机下载目录——仍属本地操作。
- 建议后续为安装包提供 **SHA-256 校验值公示**（在 Releases 说明中附带），便于用户验证二进制完整性。
- 建议为「解压到文件夹」增加**写入前二次确认 + 目标目录预览**，防止误覆盖。

## 6. 负责任披露

如发现安全漏洞，请通过 GitHub Issues 私信或仓库 Security 策略提交，标题注明 `[SECURITY]`。我们对本地优先产品的隐私问题优先处理。
